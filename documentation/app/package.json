{
  "devDependencies": {
    "@fastly/compute-js-static-publish": "^6.1.0",
    "@fastly/js-compute": "^3"
  },
  "dependencies": {
    "@h7/hono-fastly-static": "^0.2.0"
  },
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prebuild": "compute-js-static-publish --build-static --suppress-framework-warnings",
    "build": "js-compute-runtime src/index.js",
    "deploy": "fastly compute publish",
    "start": "fastly compute serve"
  }
}
